# Troubleshooting

- Import errors: check venv activation and pip install -r requirements.txt
- Slow training: reduce batch size, use smaller models, or sample data
- Data not found: follow data/README.md in each project
